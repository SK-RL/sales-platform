<!-- Loaded on demand by SKILL.md: read ONLY the file matching the job's host. -->

## Lever — `jobs.lever.co/<co>/<id>` → `/apply`
- `name` (single full-name field), `email`, `phone`, `org` (current company)
- `urls[LinkedIn]`, `urls[GitHub]`, `urls[Portfolio]`, `urls[Other]`
- Resume: `input[name=resume]` file.
- `comments` = "Additional information" textarea — this is the cover
  letter slot.
- Custom: `cards[<id>][field<n>]` inputs, labeled.
- EEO: `eeo[gender]`, `eeo[race]`, `eeo[veteran]`, `eeo[disability]` —
  each has a decline option.
- Consent checkbox for data processing is often required.
- Submit: button "Submit application".

## Verified 2026-09-10 (Istari Digital)

**"US PERSON" is a hard stop, and it is not a work-authorization
question.** Istari's form has a required "US PERSON" field whose only
options are "US Citizen" and "US Permanent Resident" — no third option.
Being authorized to work in the US does not make someone a US person;
that bar exists for US Federal Government contract work (ITAR-style).
Sarthak is an Indian citizen, so no truthful answer exists. Do not
submit. Record the blocker against the job and leave it for him.

Note the same form's separate "US CITIZENSHIP" field *does* offer "Not
a US Citizen or Permanent Resident" — don't let answering that one
carry you into answering the other.
